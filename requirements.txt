#  python>=3.8
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0  # Required for Excel export via pandas
# iSocket is a custom module included in src/instruments/iSocket.py
# pyvisa can be used as an alternative: pyvisa>=1.12.0
